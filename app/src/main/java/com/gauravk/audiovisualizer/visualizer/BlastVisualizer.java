package com.gauravk.audiovisualizer.visualizer;

import android.content.Context;
import android.util.AttributeSet;
import android.view.View;

public class BlastVisualizer extends View {
    public BlastVisualizer(Context context) {
        this(context, null);
    }

    public BlastVisualizer(Context context, AttributeSet attrs) {
        this(context, attrs, 0);
    }

    public BlastVisualizer(Context context, AttributeSet attrs, int defStyle) {
        super(context, attrs, defStyle);
    }
}
